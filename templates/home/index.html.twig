{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur MonSite{% endblock %}

{% block stylesheets %}
    <style>
        :root {
            --primary-color: #0d6efd;
            --accent-color: #20c997;
            --dark-color: #343a40;
            --light-color: #f8f9fa;
        }

        body {
            background-color: var(--light-color);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar {
            background-color: var(--dark-color);
            border-bottom: 3px solid var(--accent-color);
        }

        .navbar-nav .nav-link {
            color: #ccc;
            transition: all 0.3s ease-in-out;
            border-radius: 5px;
        }

        .navbar-nav .nav-link:hover {
            background-color: var(--accent-color);
            color: white !important;
        }

        footer {
            background-color: var(--dark-color);
            color: white;
            padding-top: 1rem;
            padding-bottom: 1rem;
        }

        main.container {
            background-color: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 0 15px rgba(0,0,0,0.05);
        }

        .card-hover {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border-radius: 12px;
        }

        .card-hover:hover {
            transform: translateY(-6px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
        }

        .btn-accent {
            background-color: var(--accent-color);
            color: white;
            border-radius: 30px;
            padding: 0.5rem 1.2rem;
            font-weight: 500;
            transition: background-color 0.3s ease, transform 0.2s ease;
            border: none;
        }

        .btn-accent:hover {
            background-color: #17a589;
            transform: scale(1.05);
        }

        a.logout-link {
            color: #dc3545;
            font-weight: bold;
            cursor: pointer;
        }

        a.logout-link:hover {
            text-decoration: underline;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="text-center mb-5">
            <h1 class="display-4 fw-bold text-primary">Bienvenue sur <span class="text-dark">FSIA</span></h1>
            <p class="lead text-muted">
                Votre plateforme dédiée à la formation, à l'information,<br>et à l'innovation grâce à l'intelligence artificielle.
            </p>
        </div>

        <div class="row text-center g-4">
            <div class="col-md-4">
                <div class="card h-100 shadow-sm border-0 card-hover">
                    <div class="card-body">
                        <h3 class="card-title mb-3">🎓 Formations</h3>
                        <p class="card-text">Développez vos compétences avec nos formations en développement web, IA, data science, et plus encore.</p>
                    </div>
                    <div class="card-footer bg-transparent">
                        <a href="{{ path('training') }}" class="btn btn-accent">Voir les formations</a>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card h-100 shadow-sm border-0 card-hover">
                    <div class="card-body">
                        <h3 class="card-title mb-3">📰 Articles</h3>
                        <p class="card-text">Restez informé avec nos articles sur l'IA, la tech, et l'actualité numérique.</p>
                    </div>
                    <div class="card-footer bg-transparent">
                        <a href="{{ path('articles') }}" class="btn btn-accent">Lire les articles</a>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card h-100 shadow-sm border-0 card-hover">
                    <div class="card-body">
                        <h3 class="card-title mb-3">💬 Chat IA</h3>
                        <p class="card-text">Posez vos questions à notre assistant IA : résumés, explications ou accompagnement personnalisé.</p>
                    </div>
                    <div class="card-footer bg-transparent">
                        <a href="{{ path('chat') }}" class="btn btn-accent">Accéder au chat</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
